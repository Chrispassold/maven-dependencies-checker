/**
 * Results Tables Component
 * Handles the display of comparison result tables
 */

window.resultsTablesComponent = function() {
    return {
        // This component inherits data from the parent comparison results component
        // The tables will be rendered using the filtered results from the parent
    };
}; 